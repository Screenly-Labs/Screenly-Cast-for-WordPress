<?php
/**
 * The degraded-mode gate.
 *
 * GENERATED by build.ts from @screenly-labs/signage-kit, do not edit. Run
 * `bun run build` to regenerate.
 *
 * Marks the document html.legacy when the engine renders below the support floor
 * or the hardware looks weak, so the stylesheet can drop animation.
 *
 * @package ScreenlyCast
 */

defined( 'ABSPATH' ) || exit;
?>
<!-- Degraded mode for older/weaker signage players (@screenly-labs/signage-kit). -->
    <script>
      (function () {
        try {
          var slow =
            (navigator.deviceMemory && navigator.deviceMemory <= 2) ||
            (navigator.hardwareConcurrency && navigator.hardwareConcurrency <= 2)
          var old = !('replaceChildren' in Element.prototype)
          if (slow || old) document.documentElement.classList.add('legacy')
        } catch (e) {
          document.documentElement.classList.add('legacy')
        }
      })()
    </script>
